export interface BookInfoProps {
    id?: string,
    title?: string,
    author?: string,
    genre?: string,
    language?: string,
    description?: string,
    publication_date?: string
    trade_format?: string,
    image_url?: string,
    user_id?: string
}